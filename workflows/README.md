# Optional workflows

This directory is reserved for optional external automation workflows.

AI Process Studio 1.1.0 does not bundle or require any third-party workflow platform. External automations must remain optional, user-configured and outside the core application contract.

A workflow may only be added here once its configuration has been validated and secrets or environment-specific identifiers have been removed.
